No compile time reference to concrete classes
RepositoryFactory returns instance using Activator class and settings in App.config file
Without compiling the application, data can be retrieved from SQL or XML. Just need to change the config file for that

IMP : Post build event is used to copy the assemblies to output directory